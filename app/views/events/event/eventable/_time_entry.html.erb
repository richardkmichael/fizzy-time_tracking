<%= render "events/event/layout", card: event.eventable.card, event: event do %>
  <span class="txt-break overflow-line-clamp txt-tight-lines">
    <%= event.eventable.duration %>
  </span>
<% end %>
